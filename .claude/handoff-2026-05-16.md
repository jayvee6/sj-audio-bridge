# SJAudioBridge handoff — 2026-05-16

**Goal:** native macOS menubar helper, ScreenCaptureKit system-audio →
localhost WebSocket → sj-audio `createNativeBridgeSource()`. Zoom/Teams-style.

**Decisions locked (with user):**
- macOS API: ScreenCaptureKit audio (`SCStream` + `capturesAudio`)
- Sequencing: macOS-first, ship, then Windows (WASAPI loopback) later vs same wire protocol
- Packaging: standalone notarized menubar app, new sibling repo `sj-audio-bridge`
- Web side: new `createNativeBridgeSource()` in existing sj-audio repo; PCM
  injected via AudioWorklet → existing AnalyserNode → existing FramePipeline
  (zero analysis-pipeline changes → automatic viz parity)

**Notarization: READY.** Validated + stored in keychain as profile
`sj-audio-bridge-notary` (Developer ID Application: Jose Villarreal
ATGGQ68RUK, key 27B9NKKUUV). B8 references the profile — no secrets, no pause.

**Scaffold via skill:** macos-spm-app-packaging (Scripts/ copied verbatim;
package_app.sh sources version.env; MENU_BAR_APP=1 → LSUIElement).

**Chunks:** B0 repo init · B1 menubar skeleton · B2 SCK TCC + content
enumeration · B3 SCStream audio capture (log RMS) · B4 CMSampleBuffer→Float32
mono PCM · B5 localhost WS + token · B6 wire protocol (JSON hello + binary
PCM + token handshake) · B7 menubar UI · B8 sign+notarize+staple · B9 README.
Then sj-audio W1–W4 (decoder, AudioWorklet injector, adapter, engine+demo+v0.2.0).

**Verify state:** `cd /Users/jdot/Documents/Development/sj-audio-bridge && swift build && git log --oneline`

**Append protocol:** after each commit append `[HH:MM] B<n>: <summary> — <sha7>`.

---

## Log

<!-- append below this line -->
[01:29] B0: repo init (SwiftPM scaffold, builds+runs) — 6f24726
[01:37] B1: menubar skeleton (LSUIElement bundle verified) — fb3087e
[01:47] B2: SCK access + enumeration (Developer ID + hardened runtime signing) — eaeb368
[01:53] B3: SCStream audio capture + RMS (build verified; runtime user-gated) — 1153e74
[01:57] B3 VERIFIED: -10.6 dBFS w/ music, -120 silent — real system audio captured (1153e74)
[02:00] B4: mono PCM blocks + BlockMeter (build verified; runtime user-gated) — 8bb8bd8
[02:11] B5 VERIFIED: loopback-only bind + WS hello (node client) — 48e79da
[02:17] B6 VERIFIED: token-gated binary PCM (node battery A/B/C all PASS) — dc2ef72
